# Generate lefse analysis for IDSA status based on corresponding shared & design files
#https://mothur.org/wiki/lefse/
set.dir(input=data/process, output=data/process, seed=19760620)

# Perform lefse on the 3 sets of input files
lefse(shared = idsa.shared, design=idsa.design)
