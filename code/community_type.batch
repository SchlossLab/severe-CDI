# Generate community type analysis based on shared file at the genus level
#Use Dirichlet Multinomial Mixtures method (the default in mothur) to create the community types
set.dir(input=data/process, output=data/process, seed=19760620)

# Shorten filenames
get.communitytype(shared = cdi.subsample.genus.shared, maxpartitions = 24, method = dmm)
